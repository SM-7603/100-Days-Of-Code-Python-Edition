# using the len() function to "find the length of a string" (the number of charaters that exist in it.)

name = input("Please enter your name: \n")

name_length = str(len(name))

print("The number of letters in your name = " + name_length)
